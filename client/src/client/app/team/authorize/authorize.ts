import {Component} from '@angular/core';

@Component({
  moduleId: module.id,
  selector:'team-authorize',
  templateUrl:'authorize.html',
  styleUrls:['authorize.css']
})
export class AuthorizeComponent {
  constructor() {
  }
}


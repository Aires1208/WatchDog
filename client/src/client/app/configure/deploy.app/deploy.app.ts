import {Component} from '@angular/core';

@Component({
  moduleId: module.id,
  selector:'deploy-app',
  templateUrl:'deploy.app.html',
  styleUrls:['deploy.app.css']
})
export class DeployAppComponent {
  constructor() {
  }
}

